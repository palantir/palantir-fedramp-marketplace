version: 2.1

jobs:
  verify:
    docker:
      - image: cimg/go:1.26.4
    steps:
      - checkout
      - run:
          name: ./godelw verify
          command: ./godelw verify --apply=false

workflows:
  verify:
    jobs:
      - verify
